# 《大模型面试100问》文档梳理

## 📚 文档概述

**文档名称**：小黄搞AI_大模型面试100问（PDF更新至74页）  
**主要内容**：涵盖大模型领域的核心技术知识点，包括模型架构、训练技术、推理优化、强化学习等多个方面

---

## 📖 内容结构总览

### 一、参数高效微调技术 (PEFT)

#### 1. LoRA (Low-Rank Adaptation)
- **核心原理**：通过低秩矩阵分解减少微调参数量
- **公式**：W_new = W + ΔW = W + (A × B)
- **初始化策略**：B矩阵初始化为零，A矩阵随机高斯初始化
- **优点**：高效性、通用性强、保持主干模型权重不变

#### 2. AdaLoRA
- **核心创新**：自适应调整低秩值
- **特点**：根据梯度信息动态分配每层的秩值
- **应用场景**：模型微调阶段

#### 3. QLoRA
- **核心创新**：结合4-bit量化和LoRA
- **特点**：显著减少计算和存储需求
- **应用场景**：模型部署阶段

#### 4. Adapter技术
- **原理**：在Transformer层间插入小型可训练模块
- **类型**：Houlsby Adapter、Parallel Adapter、Compacter
- **优势**：参数效率高、任务间参数共享

#### 5. Prefix-tuning
- **原理**：在输入序列前添加可训练的前缀向量
- **特点**：不改变模型内部结构或权重
- **应用**：文本生成任务

#### 6. P-tuning
- **原理**：优化连续的提示向量
- **特点**：连续可优化的嵌入向量作为提示
- **版本**：P-tuning v2支持多层提示

#### 7. Prompt-tuning
- **原理**：仅优化提示向量，冻结模型参数
- **特点**：参数高效，适用于大规模模型

---

### 二、训练优化技术

#### 1. 混合精度训练
- **原理**：结合FP32和FP16进行训练
- **流程**：权重FP32存储→FP16计算→梯度FP32更新
- **关键技术**：损失缩放(Loss Scaling)

#### 2. 数据类型选择
| 类型 | 特点 | 应用场景 |
|------|------|----------|
| float32 | 高精度，内存大 | 模型参数更新 |
| float16 | 计算快，易溢出 | 中间层计算 |
| bfloat16 | 范围大，精度低 | TPU训练 |

#### 3. 溢出问题解决
- 混合精度训练
- 损失缩放
- 使用bfloat16
- 梯度裁剪

---

### 三、预训练任务

#### 1. CLM (Causal Language Modeling)
- **定义**：因果语言建模，预测下一个词
- **特点**：单向上下文，自回归模型
- **应用**：GPT系列模型

#### 2. MLM (Masked Language Modeling)
- **定义**：掩码语言建模，预测被掩盖的词
- **特点**：双向上下文
- **应用**：BERT模型

#### 3. GLM自回归空白填充
- **特点**：结合自编码和自回归优势
- **优势**：避免信息泄露，统一理解和生成任务

---

### 四、模型架构

#### 1. Transformer架构
- **核心组件**：
  - 多头自注意力机制
  - 前馈神经网络
  - 层归一化
  - 残差连接
- **位置编码**：正弦余弦函数

#### 2. 架构类型对比
| 架构 | 特点 | 代表模型 | 应用场景 |
|------|------|----------|----------|
| Encoder-only | 双向理解 | BERT | 文本分类、NER |
| Decoder-only | 自回归生成 | GPT | 文本生成 |
| Encoder-Decoder | 序列到序列 | T5 | 机器翻译、摘要 |

#### 3. Decoder-only为何成为主流
- 自回归语言模型的优势
- 训练效率和可扩展性
- 统一框架处理多种任务
- 简化架构降低复杂度

---

### 五、注意力机制优化

#### 1. 多头注意力机制
- **原理**：并行计算多个注意力头
- **优势**：捕捉不同特征信息、增强表达能力

#### 2. Q、K、V的设计
- **Query**：查询向量，提出问题
- **Key**：键向量，提供匹配标准
- **Value**：值向量，传递实际信息

#### 3. Scaled Dot-Product
- **公式**：Attention(Q,K,V) = softmax(QK^T/√d_k)V
- **原因**：防止点积值过大导致梯度消失

#### 4. KV Cache
- **原理**：缓存已计算的键值对
- **阶段**：预填充阶段(计算密集) + 解码阶段(内存密集)

#### 5. GQA (Grouped-Query Attention)
- **原理**：将注意力头分组共享KV
- **优势**：平衡推理速度和模型性能

---

### 六、位置编码

#### 1. 绝对位置编码
- **方法**：正弦余弦函数
- **局限**：无法直接捕捉相对位置

#### 2. RoPE (旋转位置编码)
- **原理**：通过旋转变换嵌入位置信息
- **优势**：
  - 捕捉相对位置信息
  - 远程衰减特性
  - 适用于线性Attention

---

### 七、归一化技术

#### 1. LayerNorm vs BatchNorm
| 特性 | LayerNorm | BatchNorm |
|------|-----------|-----------|
| 归一化维度 | 单个样本所有特征 | 批次内同位置特征 |
| 适用场景 | 变长序列 | 固定输入 |

#### 2. RMSNorm
- **原理**：仅基于均方根进行规范化
- **优势**：计算简单、梯度稳定、效率更高

---

### 八、激活函数

#### 1. ReLU
- **公式**：max(0, x)
- **特点**：简单高效，但存在"死神经元"问题

#### 2. SwiGLU
- **公式**：SwiGLU(x,W,V) = Swish(xW) ⊙ (xV)
- **优势**：平滑激活、门控机制、性能更好

---

### 九、分词技术

#### 1. BPE (Byte Pair Encoding)
- **原理**：迭代合并最高频字符对
- **特点**：基于频率的贪心算法

#### 2. WordPiece
- **原理**：考虑语言模型概率的合并策略
- **特点**：生成更有意义的子词

#### 3. BBPE (Byte-Level BPE)
- **原理**：在字节层面操作
- **优势**：处理多语言文本、统一tokenization

---

### 十、主流模型架构

#### 1. BERT
- **架构**：Encoder-only
- **输入**：WordPiece + Position + Segment Embedding
- **预训练任务**：MLM + NSP

#### 2. LLaMA
- **创新点**：
  - RMS Pre-Norm
  - SwiGLU激活函数
  - RoPE位置编码
  - GQA注意力机制

#### 3. Qwen (通义千问)
- **特点**：
  - 多语言词汇表(152K)
  - SwiGLU激活函数
  - RoPE位置编码
  - 长上下文扩展技术

#### 4. LLaMA 3.1
- **创新**：
  - 三阶段预训练
  - 行级别去重
  - 拒绝采样+SFT+DPO训练策略
  - 4D并行训练

---

### 十一、推理加速技术

#### 1. FlashAttention
- **原理**：块处理 + IO感知
- **优势**：减少内存占用、提高计算效率

#### 2. vLLM
- **核心技术**：
  - PagedAttention：分页管理KV Cache
  - Continuous Batching：动态批次管理
- **性能**：比HuggingFace快14-24倍

---

### 十二、分布式训练

#### 1. ZeRO系列
| 版本 | 分区内容 | 显存优化 |
|------|----------|----------|
| ZeRO-1 | 优化器状态 | 4x |
| ZeRO-2 | 优化器状态+梯度 | 8x |
| ZeRO-3 | 优化器状态+梯度+参数 | 与GPU数成正比 |

#### 2. 并行策略
- **数据并行(DP)**：分割数据，复制模型
- **张量并行(TP)**：切分权重矩阵
- **流水线并行(PP)**：按层划分模型

---

### 十三、强化学习

#### 1. 基本概念
- **策略函数π(a|s)**：状态到动作的映射
- **值函数V(s)**：状态的期望累积回报

#### 2. PPO (Proximal Policy Optimization)
- **核心**：通过剪辑机制限制策略更新幅度
- **损失函数**：L_CLIP(θ) = E[min(r_t(θ)A_t, clip(r_t(θ),1-ε,1+ε)A_t)]

#### 3. DPO (Direct Preference Optimization)
- **原理**：直接从偏好数据优化策略
- **损失函数**：L(θ) = -E[logσ(f_θ(x_+) - f_θ(x_-))]
- **与PPO对比**：不需要显式奖励模型

---

### 十四、MoE (混合专家模型)

#### 1. 核心架构
- **门控网络**：选择专家
- **专家模型**：处理特定类型数据

#### 2. 与Dense模型对比
| 特性 | MoE | Dense |
|------|-----|-------|
| 参数激活 | 部分 | 全部 |
| 计算效率 | 高 | 低 |
| 适用场景 | 大规模并发 | 稳定性能需求 |

---

### 十五、代码实现

#### 1. Self-Attention (PyTorch)
```python
class SelfAttention(nn.Module):
    def __init__(self, dim_q, dim_k, dim_v):
        super().__init__()
        self.linear_q = nn.Linear(dim_q, dim_k, bias=False)
        self.linear_k = nn.Linear(dim_q, dim_k, bias=False)
        self.linear_v = nn.Linear(dim_q, dim_v, bias=False)
        self._norm_fact = 1 / sqrt(dim_k)
    
    def forward(self, x):
        q = self.linear_q(x)
        k = self.linear_k(x)
        v = self.linear_v(x)
        dist = torch.bmm(q, k.transpose(1,2)) * self._norm_fact
        dist = torch.softmax(dist, dim=-1)
        return torch.bmm(dist, v)
```

#### 2. Multi-Head Attention
- 多头并行计算
- 维度拆分与拼接
- 输出线性变换

---

## 📊 知识点分类统计

| 类别 | 问题数量 | 重要程度 |
|------|----------|----------|
| 参数高效微调 | 10+ | ⭐⭐⭐⭐⭐ |
| 模型架构 | 15+ | ⭐⭐⭐⭐⭐ |
| 训练优化 | 10+ | ⭐⭐⭐⭐ |
| 推理加速 | 5+ | ⭐⭐⭐⭐ |
| 强化学习 | 8+ | ⭐⭐⭐⭐ |
| 代码实现 | 5+ | ⭐⭐⭐⭐⭐ |

---

## 🎯 面试重点总结

### 必须掌握的核心概念
1. **LoRA原理与初始化**
2. **Transformer架构细节**
3. **注意力机制(Q/K/V)**
4. **位置编码(RoPE)**
5. **归一化技术(LayerNorm/RMSNorm)**
6. **混合精度训练**
7. **PPO/DPO算法**
8. **Self-Attention代码实现**

### 常见面试题型
1. **原理阐述题**：解释某技术的核心原理
2. **对比分析题**：比较不同方法的优缺点
3. **代码实现题**：手撕核心算法
4. **场景应用题**：如何选择合适的技术方案

---

## 📝 学习建议

1. **理论基础**：深入理解每个技术的数学原理
2. **代码实践**：动手实现核心算法
3. **对比分析**：建立知识体系，理解技术演进
4. **实际应用**：了解各技术的适用场景

---

*文档梳理完成，共涵盖60+核心知识点*
